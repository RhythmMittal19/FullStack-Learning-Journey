const people = ['yoshi', 'ching-hing', 'ling-ling', 'tralala'];

const ages = [20, 25, 30, 25];
// console.log(people);

// if u want to use variables or things from one file to another u have to manually export to another file

module.exports = {
  people, ages
};
